%bcond_without tests
%bcond_without weak_deps

%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')
%global __provides_exclude_from ^/opt/ros/jazzy/.*$
%global __requires_exclude_from ^/opt/ros/jazzy/.*$

Name:           ros-jazzy-ros2nodl
Version:        2.0.2
Release:        1%{?dist}%{?release_suffix}
Summary:        ROS ros2nodl package

License:        Apache-2.0
URL:            https://index.ros.org/p/ros2nodl/#jazzy
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-jazzy-ament-index-python
Requires:       ros-jazzy-nodl-conformance
Requires:       ros-jazzy-nodl-observe
Requires:       ros-jazzy-nodl-schema
Requires:       ros-jazzy-rclpy
Requires:       ros-jazzy-ros2cli
Requires:       ros-jazzy-rosgraph-msgs
Requires:       ros-jazzy-rosidl-runtime-py
Requires:       ros-jazzy-ros-workspace
BuildRequires:  python%{python3_pkgversion}-devel
BuildRequires:  ros-jazzy-ament-index-python
BuildRequires:  ros-jazzy-nodl-conformance
BuildRequires:  ros-jazzy-nodl-observe
BuildRequires:  ros-jazzy-nodl-schema
BuildRequires:  ros-jazzy-rclpy
BuildRequires:  ros-jazzy-ros-workspace
BuildRequires:  ros-jazzy-ros2cli
BuildRequires:  ros-jazzy-rosgraph-msgs
BuildRequires:  ros-jazzy-rosidl-runtime-py
Provides:       %{name}-devel = %{version}-%{release}
Provides:       %{name}-doc = %{version}-%{release}
Provides:       %{name}-runtime = %{version}-%{release}

%if 0%{?with_tests}
BuildRequires:  python%{python3_pkgversion}-pytest
BuildRequires:  ros-jazzy-builtin-interfaces
BuildRequires:  ros-jazzy-rcl-interfaces
BuildRequires:  ros-jazzy-std-msgs
%endif

%description
ros2cli command entrypoint for NoDL.

%prep
%autosetup -p1

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/jazzy/setup.sh" ]; then . "/opt/ros/jazzy/setup.sh"; fi
%py3_build

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/jazzy/setup.sh" ]; then . "/opt/ros/jazzy/setup.sh"; fi
%py3_install -- --prefix "/opt/ros/jazzy"

%if 0%{?with_tests}
%check
# Look for a directory with a name indicating that it contains tests
TEST_TARGET=$(ls -d * | grep -m1 "\(test\|tests\)" ||:)
if [ -n "$TEST_TARGET" ] && %__python3 -m pytest --version; then
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/jazzy/setup.sh" ]; then . "/opt/ros/jazzy/setup.sh"; fi
%__python3 -m pytest $TEST_TARGET || echo "RPM TESTS FAILED"
else echo "RPM TESTS SKIPPED"; fi
%endif

%files
/opt/ros/jazzy

%changelog
* Fri Sep 04 2026 ROSGraph Working Group <rosgraph-wg@googlegroups.com> - 2.0.2-1
- Autogenerated by Bloom

* Fri Apr 19 2024 Ubuntu Robotics <ubuntu-robotics@lists.launchpad.net> - 0.3.1-5
- Autogenerated by Bloom

* Wed Mar 06 2024 Ubuntu Robotics <ubuntu-robotics@lists.launchpad.net> - 0.3.1-4
- Autogenerated by Bloom

