<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>ros2nodl</name>
  <version>2.0.2</version>
  <description>ros2cli command entrypoint for NoDL.</description>
  <maintainer email="rosgraph-wg@googlegroups.com">ROSGraph Working Group</maintainer>
  <license>Apache-2.0</license>
  <author email="hello@alistairenglish.com">Alistair English</author>
  <author email="me@emersonknapp.com">Emerson Knapp</author>

  <depend>nodl_observe</depend>
  <depend>nodl_conformance</depend>
  <depend>nodl_schema</depend>
  <depend>rosgraph_msgs</depend>
  <depend>ros2cli</depend>
  <!-- The describe verb shells out to the nodl_observe `observe` binary and
       renders its latched Node: rclpy to receive, rosidl_runtime_py to render,
       ament_index_python to locate the binary.  (Previously transitive via the
       Python nodl_observe, which is now C++.) -->
  <depend>rclpy</depend>
  <depend>rosidl_runtime_py</depend>
  <depend>ament_index_python</depend>

  <test_depend>python3-pytest</test_depend>
  <test_depend condition="$ENABLE_PIP_TEST_DEPENDS == 1">python3-pyright-pip</test_depend>
  <test_depend>std_msgs</test_depend>
  <!-- Describe tests exercise generated messages and observe's MCAP fixtures. -->
  <test_depend>rcl_interfaces</test_depend>
  <test_depend>builtin_interfaces</test_depend>
  <test_depend condition="$ENABLE_PIP_TEST_DEPENDS == 1">mcap-ros2-support</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
